name: simplenote
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  
  # Package yang sudah ada:
  url_launcher: ^6.2.5
  table_calendar: ^3.0.9
  google_maps_flutter: ^2.6.0
  geolocator: ^10.1.0
  http: ^1.2.0
  provider: ^6.0.5
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  sqflite: ^2.3.2 
  path: ^1.8.3
  cupertino_icons: ^1.0.8
  geocoding: ^4.0.0
  
  # TAMBAHKAN INI:
  permission_handler: ^11.0.0  # Untuk permission lokasi

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  # HAPUS SALAH SATU flutter_lints, sisakan hanya satu:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true